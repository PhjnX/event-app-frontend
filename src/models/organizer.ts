export interface Organizer {
  organizerId: number;
  slug: string;
  name: string;
  description: string;
  logoUrl: string;
  contactPhoneNumber: string;
  contactEmail: string;
  userId: number;
  username: string; 
  approved: boolean; 
}
